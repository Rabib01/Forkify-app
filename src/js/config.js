// all sort of constansthat should be reused throughout the project
//forexample the api Url that is going to be usedat multiple places forthe project to workif the url suddenly needsto changelfor examplebecomev3from a v2then wewouldneed to change that in multile placesandso we do not wantthat to happen there we keep thishereasaconstant

export const API_URL = `https://forkify-api.jonas.io/api/v2/recipes/`; // named export
export const TIMEOUT_SECONDS = 10;
export const RES_PER_PAGE = 10;
